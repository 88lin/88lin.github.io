# 88lin 个人主页

此目录是独立的静态网站，无需安装前端依赖或构建。保留原有居中大标题、黄色高亮、悬浮胶囊导航、MacBook 终端、星光动效和粉彩卡片。

## 本地预览

在仓库根目录运行：

```powershell
python -m http.server 8765 --bind 127.0.0.1
```

打开 `http://127.0.0.1:8765/2/`。关于页为 `/2/about/`，404 页面为 `/2/404.html`。404 的自动托管行为由部署平台配置决定。

## 文件与维护

- `index.html`：首页内容、项目展示、原有样式与终端动画。
- `about/index.html`：关于页，保留原有内容与视觉设计。
- `404.html`：错误页；本地资源使用 `/2/` 绝对路径，避免嵌套错误地址解析失败。
- `assets/enhancements.css`：基于原设计的布局、粉彩项目卡片、移动端与无障碍修正。
- `assets/avatar.webp`：由已有 `icon.png` 转换的头像，尺寸 256 × 256；原 PNG 继续用于 Apple Touch Icon。
- `llms.txt`：由页面真实内容生成的个人主页索引，包含项目、站点、文章与作者资料。
- `scripts/build-seo.py`：使用 Python 标准库同步静态 JSON-LD、项目片段地址和内容索引；仅维护时运行。
- `SEO-GEO.md`：SEO / GEO 修改依据、验证记录与发布后的检查步骤。

项目以静态 HTML 交付，不依赖访问者请求 GitHub API。首页先展示 8 个项目，原生 `details` 可展开另外 6 个；关闭 JavaScript 也可浏览全部项目。添加精选项目时更新项目卡片及展开提示；首屏项目总数使用 GitHub 账户公开仓库总数，不等于精选卡片数量。

项目信息于 2026-09-20 核对 GitHub 公开 API、仓库 README 和演示地址。首屏统计为 5,865 Stars、118 个公开仓库，包含 Fork；按 GitHub owner 仓库接口遍历全部分页求和，仓库自身的 Star 数不会继承上游。Stars 是当日快照，更新时同时修改页面注明的日期和单项目徽标。`react-ai-orb` 标注二次开发，`gzh-design-skill` 标注基于上游维护；不把 `fork=false` 等同于原创。TextCard 的有效地址为 `https://88lin.github.io/TextCard-Studio/`。

## 本次验证

- 首页、关于页、404 页使用 Chromium 检查 320、375、390、430、640、768、844（横屏）、1024、1440 px，共 27 组页面与视口组合；未发现横向溢出、内容裁切或页面脚本错误。
- 验证项目展开与收起、键盘操作、跳转到主要内容、终端打字动画、运行天数显示、减少动态效果偏好和无 JavaScript 降级。
- 站点区保留 6 个独立入口；维修助手与 Lofi 只在项目区展示。首页只保留一组统计，个人详细介绍放在关于页。
- 首页精选 6 篇已核对正文的文章：实用生活指南、Notion 快捷键、李笑来作品集、Windows 实用教程、C 盘清理、Anything You Want 创业短课。外部博客原文没有删除。
- 首页与关于页共用新版页脚，包含订阅、友链、回到顶部和备案信息，已移除设计署名。
- 本地资源引用、内联 JavaScript 语法与 `git diff --check` 通过；本次浏览器检查未发现控制台异常或资源错误。

## 关于页与移动端排版

- 延续居中问候、黄色高亮、粉彩卡片与头像。介绍、时间线、技能区在宽屏采用标题与内容两列布局，800 px 以下恢复顺序阅读；关注方向以宽屏双列、手机单列展示。
- 关于页相邻区块原有 140 px 留白收紧为桌面约 88 px、手机 64 px，正文段落间距 16 px。首页主要区块上下内边距为 48–72 px，标题与内容间距为 24–32 px，避免随屏幕高度不断拉大空白。
- 手机导航保留头像返回首页入口，导航与联系按钮的高度不低于 44 px。联系区的邮箱占一行，另外四个入口两列排列；文章分类在手机上也保持可见。
- 对比更新前的源文件，三个页面的正文和所有链接目标均已保留；介绍段仅拆分长段落，时间线补充有序列表语义。
- 黄色小标题文字加深，手机页脚、统计来源与文章分类至少为 12 px。原有衬线字体、粉彩配色、胶囊标签与终端装饰按既定视觉风格保留。

## SEO / GEO 维护

正式地址为 `https://go.88lin.eu.org/2/` 与 `https://go.88lin.eu.org/2/about/`，canonical、分享网址和站点地图已统一。根目录 `robots.txt` 才是当前部署下实际生效的抓取配置；根目录的 sitemap、内容索引和个人主页入口也需要随修改一起发布。

更改项目、文章、技能或页面元信息后，在仓库根目录运行 `python 2/scripts/build-seo.py --updated YYYY-MM-DD`，日期使用实际内容核对日期。生成结果直接提交为静态 HTML / 文本，不需要前端构建，也不依赖访问者执行 JavaScript。

本轮额外完成 JSON-LD / Schema.org 字段、目录与可见内容一致性、canonical / sitemap、抓取规则、无 JavaScript 项目直达和 12 组浏览器尺寸复核。详细范围与发布后验证见 [SEO-GEO.md](SEO-GEO.md)。
